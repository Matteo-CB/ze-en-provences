/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["svwg0rrsfacftscb.public.blob.vercel-storage.com"],
  },
  eslint: {
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
